from enum import Enum

class DifficultyLevel(Enum):
    """
    Enum representing the difficulty levels for word and bingo cards.
    Used to distinguish between easy, medium, and hard modes throughout the application.
    """
    
    EASY = "easy"
    MEDIUM = "medium"
    HARD = "hard" 